"""Tests for the prediction model."""



""" @pytest.fixture
def data():


def test_forecast():
    data = pd.read_csv('test_data.csv', index_col=0, parse_dates=True)
    model = fit_model(data['UNRATE'], model_type='hw')
    forecasts = forecast(data['UNRATE'], model)
    assert len(forecasts) == len(data) - int(len(data) * 0.7)
    assert isinstance(forecasts, pd.DataFrame)
    assert isinstance(forecasts.index, pd.DatetimeIndex)
 """
